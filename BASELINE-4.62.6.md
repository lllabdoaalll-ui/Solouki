# Solouki Baseline 4.62.6

## هذه النسخة تعالج مشكلة الإشعارات بعد منتصف الليل
النسخة 4.62.5 كانت تستخدم `toISOString().slice(0,10)` في عدة أماكن. هذا يحسب التاريخ بتوقيت UTC، لذلك في مصر بعد منتصف الليل يمكن أن تعود القيمة إلى تاريخ الأمس. تم استبداله بحساب التاريخ المحلي.

## الإرسال اليدوي الجماعي
صفحة `notifications.html` تفتح أول محادثة في نافذة باسم `solouki_whatsapp`، ثم تعيد استخدام نفس نافذة واتساب لبقية الطلاب. يجب على الأخصائي الضغط على إرسال داخل واتساب ثم الضغط على «التالي» في سلوكي.

> ملاحظة: هذه النسخة لا تحتاج Meta Cloud API للإرسال اليدوي، ولا تعتبر فتح `wa.me` دليلاً على نجاح إرسال API.

# Solouki Baseline 4.69.1

## إصلاحات فوق 4.69.0

### 1) دليل المعلمين — إصلاحات تشغيل
- إزالة عمود `name` غير الموجود من استعلام `stages` (كان يسبب خطأ 400).
- إظهار رابط **دليل المعلمين** في لوحة التحكم للمسؤول العام ومسؤول الحاسب (إزالة كلاس `is-nav-hidden` وليس فقط خاصية `hidden`).

### 2) استيراد معلمين من Excel
- زر «نموذج Excel» و«استيراد Excel» في `teachers.html`.
- الأعمدة: Teacher ID + اسم المعلم (دعم عناوين عربية/إنجليزية).
- بعد الاستيراد: تحديد الفصول يدويًا من شاشة التعديل.

### 3) SQL (تذكير)
- `stage_id` في `teacher_class_assignments` من نوع **text** ليطابق `stages.id`.
- إن لم تكن قد نفّذت الإصلاح السابق، استخدم السكربت الموجود في `sql/phase4-step69-teacher-directory.sql` (محدّث).

## ما لم يتغير
- كتالوج المخالفات للمعلمين (STEP 68) كما هو.
- تسجيل المخالفة/التكريم من المعلم غير مفعّل بعد (قادم في STEP 70+).

# Solouki Baseline 4.60.2 — STEP 60.2

خط الأساس التراكمي بعد إصلاح إسناد الفصول للأخصائي.

## ما يتضمنه هذا الإصدار
- كل ما في 4.60.0 / STEP 60
- **إصلاح:** ظهور الفصول في نافذة تعديل الأخصائي (`roles.html`)

## الإصلاح (STEP 60.2)
**المشكلة:** قائمة الفصول في تعديل الأخصائي كانت تُبنى من جدول `counselor_class_assignments` فقط (الإسنادات السابقة). إذا لم يُسند أي فصل من قبل، تظهر الرسالة «ستظهر الفصول بعد رفع الطلاب في المرحلة 3» رغم وجود طلاب وفصول في صفحة الطلاب.

**الحل في `js/roles.js`:**
1. إضافة `S.availableClasses` وتحميلها عند `boot`.
2. دالة `loadAvailableClasses()`:
   - تحاول أولاً `rpc('list_bulk_class_options')`
   - وإن فشلت: استعلام distinct من جدول `students` (stage_id, grade, class_name, section)
3. في `openUser` عند الدور = counselor: عرض كل الفصول المتاحة مع تحديد المُسند مسبقاً.
4. عند الحفظ: أخذ `section` من الفصل المختار إن وُجد (بدل تثبيت `arabic` فقط).

## لا يتطلب SQL جديد
لا حاجة لتشغيل سكربت SQL إضافي لهذا الإصلاح.

## نقطة الانطلاق
هذه النسخة هي خط الأساس الرسمي لأي إصلاح أو تطوير لاحق.

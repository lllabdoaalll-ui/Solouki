# Solouki — Roadmap الهندسي

## الحالة
| STEP | الوصف | الحالة |
|------|--------|--------|
| 0–3 | قاعدة البيانات، مصادقة، أدوار، طلاب، إشعارات واتساب | ✅ |
| 40 | PIN Security Hardening | ✅ |
| 41 | صلاحيات مرنة + تعديل طالب | ✅ |
| 42 | Foundation Hardening (تنظيم + Core) | ✅ |
| **43** | **كتالوج المخالفات والعقوبات (UI)** | ✅ الحالي |
| 44–45 | تسجيل المخالفة فردي + جماعي + تطبيع أسماء | ✅ |
| **46** | **ملف سلوك الطالب + إشعار واتساب** | ✅ الحالي |
| 46 | بوابة ولي الأمر | ⏳ |
| 47 | متابعات + قواعد تصعيد | ⏳ |
| 48 | تكريمات + نقاط | ⏳ |
| 49 | تحليلات ولوحات | ⏳ |
| 50+ | PWA، أداء، نسخ احتياطي، صقل إنتاج | ⏳ |

## قواعد التقدم
1. لا ننتقل لـ STEP جديد قبل اعتمادك العملي للسابق.
2. كل STEP هدف واحد رئيسي.
3. التسليم = ZIP تراكمي كامل + CHANGELOG.
4. الأمان والصلاحيات قبل الميزات الجديدة عند التعارض.

## STEP 43 (تم)
- صفحة `catalog.html` + `js/catalog.js` + `css/catalog.css`
- عرض الدرجات الأربع + المخالفات + العقوبات + المصفوفة + الأماكن
- قراءة فقط لمعظم الأدوار (سياسات RLS جديدة لدور authenticated على الجداول
  الخمسة — كانت مقصورة على anon فقط)؛ إدارة محدودة للمسؤول العام:
  إضافة/إيقاف مخالفة مخصصة (درجة 1–3 فقط)، إضافة/إيقاف مكان مخصص.
  الدرجات والعقوبات والمصفوفة ثابتة بنص اللائحة ولا تُدار من الواجهة.
- `sql/phase4-step43-catalog.sql`
- استخدام `js/core/*` بالكامل منذ البداية (config/utils/supabase-client/session/permissions)

## STEP 44 (القادم)
- تسجيل المخالفة الفعلي (نطاق الفصول حسب إسناد الأخصائي)
- يعتمد على كتالوج STEP 43 + جدول `violation_records` (موجود أصلاً في Schema v1.0)
- ربط تسجيل المخالفة بمسار الإشعار اليدوي عبر واتساب (`notifications.html`)
